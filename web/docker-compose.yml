version: '3.7'
services:
  db:
    image: mysql:8.0.17
    restart: always
    env_file:
      - mysql.env
    ports:
      - '3306:3306'
    expose:
      # Opens port 3306 on the container
      - '3306'
    volumes:
      - db:/var/lib/mysql
volumes:
  db: